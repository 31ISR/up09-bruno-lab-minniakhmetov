info:
  name: /api/items/:id
  type: http
  seq: 8

http:
  method: GET
  url: "{{URL}}/api/items/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
